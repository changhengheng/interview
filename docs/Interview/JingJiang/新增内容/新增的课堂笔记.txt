树状列表数据懒加载：
	三级列表每一级的id不能重复
	treeconfig
虚拟滚动列表
	大量数据渲染去用，只能用这个
	一上来不会显示所有的数据，自始至终只显示可视区域里的数据
列表排序
拖拽排序
	只适合少量数据（比如分页），大量数据拖拽会延迟会有卡顿现象，这是sortable本身的缺陷
	把需要拖拽的真实DOM（审查元素看最终渲染出来的真实DOM）的根节点放入第三方包提供的组件里，提供了两个下标，删除原来的数据，添加到新的位置
导出PDF
富文本编辑器	

- 代码书写规范
- 版本管理提交（commit）的规范

地图：百度地图PC端






















































